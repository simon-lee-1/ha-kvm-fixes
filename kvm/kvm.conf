# /etc/modprobe.d/kvm.conf
# Disable busy-poll in KVM to reduce idle CPU overhead.
# Without this, the host CPU stays at ~30% even when the VM is idle.
# With this, idle drops to ~10%.
options kvm halt_poll_ns=0
